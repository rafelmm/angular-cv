'use strict';

// Declare app level module which depends on filters, and services
angular.module('cvApp', [
  'ngRoute',
  'cvApp.filters',
  'cvApp.services',
  'cvApp.directives',
  'cvApp.controllers'
]);
